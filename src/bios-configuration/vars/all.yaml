---
# vars file

# These are the recommended IP addresses in the OpenShift Reference
# Architecture, so OK to leave as-is if using the same IP addresses.

# The bastion node will serve as NFS server but need to define the IP
# address in the _management_ network.
nfs_server: 192.168.8.31
nfs_subnet: 192.168.8.0/24
nfs_share: /nfs_share

# Don't edit values below
SCP_R640_bios: SCP_R640_bios.json
SCP_R740xd_bios: SCP_R740xd_bios.json
SCP_one_time_boot: SCP_one_time_boot.json

