apiVersion: v1
kind: ConfigMap
metadata:
  name: demo
  namespace: isilon
data:
  sample: file data
---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: isilon
spec:
  containers:
  - name: task-pv-container
    image: quay.io/centos/centos
    ports:
      - containerPort: 80
        name: "http-server"
    volumeMounts:
      - mountPath: "/mnt"
        name: vol
      - mountPath: "/tmp"
        name: confmap
    command: ["/bin/bash", "-c", " sleep 2000000000"]
  nodeName: <node_name>
  volumes:
    - name: vol
      persistentVolumeClaim:
        claimName: pvc-replication
    - name: confmap
      configMap:
        name: demo
        items:
        - key: sample
          path: keys
