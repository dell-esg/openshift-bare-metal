---
apiVersion: v1
kind: Service
metadata:
  name: svs-sts-busybox-resiliency
  labels:
    app: sts-busybox-resiliency
spec:
  ports:
  - port: 3306
  clusterIP: None
  selector:
    app: sts-busybox-resiliency
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sts-busybox-resiliency
  labels:
    app: sts-busybox-resiliency
spec:
  serviceName: "svc-sts-busybox"
  selector:
    matchLabels:
      app: sts-busybox-resiliency
  replicas: 1
  template:
    metadata:
      labels:
        app: sts-busybox-resiliency
        podmon.dellemc.com/driver: csi-powerstore
    spec:
      containers:
      - name: busybox
        image: registry.k8s.io/busybox
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        command: [ "sh", "-c" ]
        args: [ "while true; do echo $(date):$NODE_NAME:$POD_NAME >> /data/datafile; sleep 1; done;" ]
        volumeMounts:
        - name: data
          mountPath: /data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 8Gi
      storageClassName: dellcsi-powerstore-sc 
