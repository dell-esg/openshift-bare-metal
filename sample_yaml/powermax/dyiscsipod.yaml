apiVersion: v1
kind: Pod
metadata:
  name: dynamic-iscsi-pmax-pod
  namespace: powermax
spec:
  containers:
  - name: task-pv-container
    image: nginx:latest
    ports:
      - containerPort: 80
        name: "http-server"
    volumeMounts:
      - mountPath: "/usr/share/nginx/html"
        name: dy-iscsi-pmax
  volumes:
    - name: dy-iscsi-pmax
      persistentVolumeClaim:
        claimName: dynamic-iscsi-powermax-pvc
