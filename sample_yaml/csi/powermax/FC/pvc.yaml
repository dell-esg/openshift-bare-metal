apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-fc-powermax-pvc
  namespace: powermax
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: powermaxscfc
