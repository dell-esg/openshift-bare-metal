apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: powerstore-restore
spec:
  storageClassName: powerstore-nfs
  dataSource:
    name: powerstore-snapshot
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
