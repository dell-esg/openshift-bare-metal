apiVersion: v1
kind: Pod
metadata:
  name: static-fc-pmax-pod
  namespace: powermax
spec:
  containers:
  - name: task-pv-container
    image: nginx:latest
    ports:
      - containerPort: 80
        name: "http-server"
    volumeMounts:
      - mountPath: "/usr/share/nginx/html"
        name: st-fc-pmax
  volumes:
    - name: st-fc-pmax
      persistentVolumeClaim:
        claimName: static-pvc-powermax-fc
