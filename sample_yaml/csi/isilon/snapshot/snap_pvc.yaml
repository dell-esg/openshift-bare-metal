apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: isilon-restore
spec:
  storageClassName: isilon-sc
  dataSource:
    name: isilon-snapshot
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
