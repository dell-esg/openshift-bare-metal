apiVersion: v1
kind: Pod
metadata:
  name: nfs-powerstore-pod
  namespace: powerstore
spec:
  containers:
  - name: task-pv-container
    image: nginx:latest
    ports:
      - containerPort: 80
        name: "http-server"
    volumeMounts:
      - mountPath: "/usr/share/nginx/html"
        name: dy-nfs-unity
  volumes:
    - name: dy-nfs-unity
      persistentVolumeClaim:
        claimName: nfs-powerstore-pvc
