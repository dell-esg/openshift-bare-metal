kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: odf-cephfs-daemonset-pvc
  namespace: odf-test
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: ocs-storagecluster-cephfs
  volumeMode: Filesystem
