apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dynamic-nfs-isilon-pvc
  namespace: isilon
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
  storageClassName: isilon-sc
